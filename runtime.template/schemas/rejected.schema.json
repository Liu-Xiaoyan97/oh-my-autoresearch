{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Rejected Schema",
  "description": "约束 runtime/knowledges/rejected.json",
  "type": "array",
  "items": {
    "type": "object",
    "required": ["method", "reason", "metric"],
    "properties": {
      "method": { "type": "string" },
      "reason": { "type": "string" },
      "metric": { "type": "number" },
      "timestamp": { "type": "string", "format": "date-time" }
    }
  }
}
